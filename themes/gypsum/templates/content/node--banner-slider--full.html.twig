{{ title_prefix }}
  {% if not page %}
    <h2 class="visually-hidden">
      
    </h2>
  {% endif %}
{{ title_suffix }}
            <li class="slide-item">

                <img src="{{ content.field_image.0 }}" alt="">
                <div class="caption">
                    <div class="container">
                        {{ content.body.0 }}
                        <p class="btn-row">
                            <a href="content.field_url.0" class="btn secondary-btn">Learn More</a>
                        </p>
                    </div>
                </div>
            </li>
         